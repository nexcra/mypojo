<?xml version="1.0" encoding="UTF-8"?>
<ehcache>
    <diskStore path="java.io.tmpdir" />

	<!-- name 객체의 풀 클래스 경로 -->
	<!-- maxElementsInMemory 객체의 개수 -->
	<!-- eternal true이면 캐시에 저장된 객체가 삭제되지 않는다. -->
	<!-- overflowToDisk 캐시가 최대치에 다르면 다른 디스크에 담을것인지 -->
	<!-- memoryStoreEvictionPolicy LRU : 오래된, LFU : 사용빈도가 낮은-->
	<!-- timeToIdleSeconds 마지막 접근 후 얼마나 캐시에 남을것인가 (초)-->
	<!-- timeToLiveSeconds 캐시 저장 후 얼마나 캐시에 남을것인가 (초)-->
	<!--  LRU : 오래된, LFU : 사용빈도가 낮은-->
    <defaultCache maxElementsInMemory="50000"  eternal="false"
        timeToIdleSeconds="300" timeToLiveSeconds="600" overflowToDisk="true"
        diskPersistent="false" diskExpiryThreadIntervalSeconds="120"
        memoryStoreEvictionPolicy="LRU">
    </defaultCache>
</ehcache> 